#
# This controller exists in social_stream, but it's replaced by FollowsController in ViSH
#
# To avoid messing things up, and to have views which call this controller raise an exception,
# we hereby declare this controller empty
#
class ContactsController < ApplicationController
end
