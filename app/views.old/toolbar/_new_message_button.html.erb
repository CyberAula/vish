<% if not user_signed_in? or (current_subject != subject) %>
  <%= link_to(image_tag("icons/icon-message.png",:class => "imgnoradio")+ t('  ')+ t('send.send'), new_message_path(:receiver => subject.slug), :class => "btn-gray margin-top10", :remote=> false) %>
<% end %>

