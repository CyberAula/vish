<% if activity.is_root? %>
  <%= render partial: "activities/root",
             locals: {
               activity: activity,
               root_avatar_thumb: :contact
             } %>
<% else %>
  <%= render partial: "activities/child",
             locals: {
               activity: activity,
               child_avatar_thumb: :actor
             } %>
<% end %>
