<% includeScriptTag ||= false %>

<% if includeScriptTag %>
  <script type="text/javascript">
<%end%>

$("div.applyBackstretch").each(function(index,div){
  $(div).removeClass("applyBackstretch");
  var imgUrl = $(div).attr("bs-img");
  if (typeof imgUrl != "undefined"){
    $(div).attr("bs-img");
    $(div).backstretch(imgUrl);
  }
});

<% if includeScriptTag %>
  </script>
<%end%>