<%= div_for contact, :class => 'row' do %>
  <ul class="thumbnails span3">
    <li class="span1">
      <%= link_to(image_tag(contact.receiver.logo.url(:profile)),
                  contact.receiver_subject, :class => "container") %>
    </li>
    <li class="span">
      <div class="caption elipsis">
 <div class="title_suggestion">
        <h4><%= link_to(truncate_name(contact.receiver_subject.name), contact.receiver_subject, :class => "contact_link", :length => 18) %></h4>
      </div>
      <a class="close_suggestion"> <%= link_to(image_tag('close.png'), contact, :method => :delete, :remote => true )%></a>
      <div class="follwrs-ing">
        <div class="follwrs upfoll">followers: <%= link_to contact.receiver_subject.followers.count, "#" %></div>
        <div class="folling upfoll">followings: <%= link_to contact.receiver.following_actor_objects.includes(:actor).count, "#" %></div>
      </div>
      <div class="send_messag size10 red-2 upfoll users_btn_follow"><%=contact_to(contact.receiver) %></div>
      </div>
    </li>
  </ul>
  <div class="space_center">
  </div>
<% end %>


