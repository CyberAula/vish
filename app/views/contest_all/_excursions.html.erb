<ul class="items">
  <% entities_tagged_with(["Excursion"],"ViSHCompetition2013").each do |resource| %>
    <%= render :partial => 'entities/entity', :locals => { :entity => resource, :prefix_id => "contest" } %>
  <% end %>
</ul>
