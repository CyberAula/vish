<% unless profile_subject.blank? %>
  <% res = Kaminari.paginate_array(subject_resources(profile_subject, { :limit => 0, :scope => :me })).page(params[:page]).per(12) %>
  <%= render :partial => 'common_documents/index', :locals => { :documents => res } %>
<% end %>
