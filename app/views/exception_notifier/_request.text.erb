* URL        : <%= raw ApplicationController.helpers.safe_encode @request.url %>
* HTTP Method: <%= raw @request.request_method %>
* IP address : <%= raw @request.remote_ip %>
* Parameters : <%= raw ApplicationController.helpers.safe_encode @request.filtered_parameters.inspect %>
* Timestamp  : <%= raw Time.current %>
* Server : <%= raw Socket.gethostname %>
<% if defined?(Rails) && Rails.respond_to?(:root) %>
* Rails root : <%= raw Rails.root %>
<% end %>
* Process: <%= raw $$ %>