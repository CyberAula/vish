<div class="row btn-toolbar">
  <div class="pull-right">
    <% filter_id ||= "excursion-filter" %>
    <input id="<%= filter_id %>" type="text" class="input-medium inline-block ontop" placeholder="<%= t('filter') %>" name="filter">
    <div class="btn-group">
      <%= link_to( '+'+t('options.add'), new_excursion_path, :class => 'btn', :alt =>  t('options.add'), :title => t('options.add'))  %>
      <!--
      <ul class="dropdown-menu">
        <li><%#= link_to('Virtual meeting', new_excursion_path)  %></li>
        <li><%#= link_to('presentation', new_excursion_path)  %></li>
      </ul>
      -->
    </div>
  </div>
</div>
<% content_for :javascript do %>
  if(!Vish.Filter.loaded) Vish.Filter.init();
<% end %>
