<div id="excursions_tab_ready" class="excursions">
	<ul>
		<% if profile_subject_is_current? and page===1%>
			<%= render partial: 'excursions/excursion-add' %>
		<% end %>
		<%= render partial: 'excursions/excursions_profile', :locals => { :scope => scope, :limit => limit, :page => page, :sort_by=> sort_by} %>   
	</ul> 
</div>
