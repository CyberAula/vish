<%= render :partial => 'vish_editor' %>

<script>	
	//first function called when page loads
	function start(){
		VISH.Editor.init({"token":"<%= form_authenticity_token %>", "postPath": "<%= excursion_path @excursion%>" }, <%= raw @excursion.to_json%>);		
	}	
</script>