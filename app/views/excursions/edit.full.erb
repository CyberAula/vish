<%= render :partial => 'vish_editor' %>

<script>	
	//first function called when page loads
	function start(){
		VISH.Editor.init({"token":"<%= form_authenticity_token %>", "postPath": "<%= excursions_path :format=> "json"%>" }, '<%=@excursion.as_json%>');		
	}	
</script>