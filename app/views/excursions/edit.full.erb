<% content_for :headers do%>
  <!--[if gte IE 7]>
	<%= stylesheet_link_tag "ie/editorexplorer9" %>
  <![endif]-->
<%end%>

<%= render :partial => 'vish_editor' %>

<script>	
	//first function called when page loads
	function start(){
		VISH.Editor.init({"token":"<%= form_authenticity_token %>", "postPath": "<%= excursion_path @excursion, :format => "json"%>" }, <%= raw @excursion.to_json%>);		
	}	
</script>