<% content_for :title do%>
  <%= t('welcome', :site => t('site.name')) %>
<%end%>
<section name="main">
  <div class="row span12">
    <div id="logo-frontpage"></div>
    <div class="pagination-centered name size30"><%=t('site.name')%></div>
    <div class="pagination-centered">
        <%= render :partial => 'layouts/search' ,:locals => { :sizeFieldSearch => "input-xxlarge" } %>
    </div>
  </div>
  <div class="row span12 space_double_center"></div>
  <div  id="tags" class="row span12">
    <div id="tag_repositories"class="size60"><%=t('repository.other')%></div>
    <div id="tag_excursion" class="size80"><%=t('excursion.other')%></div>
    <div id="tag_organizations"class="size45"><%=t('organization.other')%></div>
    <div id="tag_users" class="size30"><%=t('user.other')%></div>
  </div>
  <div class="row space_double_center"></div>
</section>

