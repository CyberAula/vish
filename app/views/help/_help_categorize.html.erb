<span class="helpheader">   
<i class="fa fa-question-circle hostias" data-placement='bottom' aria-hidden="true" data-original-title="<%= t('categories.actions.verb')%>" href='#' value="" ></i>
<div class="salmon" style="display: none">
  <div>
    <p> <%= t('document.help.categorize_html')%> </p>
  </div>
  <div class="popover-footer popoverfix"><%= t('document.help.overview_html')%></div>
</div>
</span>
<script>
$(document).ready(function(){
  $('.hostias').popover({ 
    html : true,
    content: function() {
      return $('.salmon').html();
    }
  });
});
</script>

<script>
$('body').on('click', function (e) {
    $('.hostias').each(function () {
        //the 'is' for buttons that trigger popups
        //the 'has' for icons within a button that triggers a popup
        if (!$(this).is(e.target) && $(this).has(e.target).length === 0 && $('.popover').has(e.target).length === 0) {
            $(this).popover('hide');
        }
    });
});
</script>