<section id="networking_tab_ready" class="excursions">
  <ul class="">
    <%= render partial: 'excursions/excursion-add' %>  
    <%= render partial: 'net_min', :locals => { :scope => scope, :limit => params[:limit], :page => params[:page], :sort_by=> sort_by, :prefix_id=> prefix_id} %>
  </ul>
</section>

