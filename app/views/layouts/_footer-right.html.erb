<nav class="span3 footer_size_text">
  <ul class="size10 nomargin">
    <li class="link_gray"><%= t('site.name') %> &nbsp;©&nbsp;<%= Date.today.year %></li>

    <li><%= link_to t('conditions.other'), '/legal_notice', :target=>'_blank', :class=>"link_blue" %></li>
    <li><%= link_to t('help'), '/help' %></li>
    <li>
      <div class="footer_btn">
        <div class="btn-group dropdown ">
          <button data-toggle="dropdown" class="btn dropdown-toggle link_gray ">
          <span class="gray size12"><%=t('lang.title')%></span>
          <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
          <% for locale in (I18n.available_locales) -%>
            <% next if locale.to_s == "pt" %>
            <li class="link_white unmargin">
              <%= link_to image_tag("flags/" + locale.to_s + ".png", :title=>locale.to_s, :class=> "unborder") + " " + t(:language_name, :locale => locale.to_s), session_locale_path(:new_locale => locale.to_s), :method => :post, :remote => false %>
            </li>
          <% end -%>
        </ul>
      </div>
      </div>
    </li>
  </ul>
</nav>

<%= render :partial => 'layouts/init_modals' %>
