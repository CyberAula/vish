<div class="row" id="experience_info">
  <div class="space_center"></div>
  <div class="header">
    <div class="header_text2">
      <%= t('profile.' + @profile.subject.class.to_s.downcase + '.experience') %>
      <% if is_owner? %>
        <div class="header_icon_right">
          <%= link_to (image_tag('icons/edit_icon.png', :class=>"unborder")), :controller => :profiles, :action => :edit, :section => "my_experience" %>
        </div>
      <% end %>
    </div>

  </div>
  <div class="row span6 boxy_white">
    <div class="space_center"></div>
    <div class="content">
      <% if @profile.experience? %>
        <div class="row">
          <div class="field_profile">
            <%= h @profile.experience %>
          </div>
        </div>
      <% elsif is_owner? %>
        <div id="field_profile_field_empty">
        <%= t('profile.empty') %>
        </div>
      <% end %>
      <div class="space_center"></div>
    </div>
  </div>
</div>
