<ul class="repository-item items">
    <% if profile_subject_is_current?%>
      <%= render partial: 'repositories/add_new' %>
    <% end %>
    <% default_sort_by = profile_subject_is_current? ? "updated_at" : "popularity"%>
    <%= render partial: 'repositories/profile_resources_page', :locals => { :scope => scope, :limit => limit, :page => page, :sort_by=> params[:sort_by]||default_sort_by} %>
</ul>
