New request for create private student groups

<%= form_for ServiceRequest::PrivateStudentGroup.new, :url => service_requests_private_student_groups_path do |f| %>
	<%= f.hidden_field :owner_id, :value => current_subject.actor_id %>
	<%= f.text_field :description %>
	<%= f.file_field :attachment %>
	<%= f.submit :class => "btn btn-primary form-sender", "data-loading-text" => t('button.sending') %>
<% end %>