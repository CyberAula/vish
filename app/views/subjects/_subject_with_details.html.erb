<div class="row-fluid">
  <div class="span2 pagination-centered link_white">
    <%= link_to(image_tag(subject.logo.url(:contact)), subject, :class => "container") %>
  </div>
  <div class=" link_white">
    <h4><%= link_to truncate_name(subject.name, :length => 40), subject %></h4>
    <h5 class="blue-white"><span class="size13"><%=t "#{subject.class.name.underscore}.one"  %></span></h5>
    <h5><%=t('follow.followers')%>: <%=num_followers%></h5>
  </div>
</div>