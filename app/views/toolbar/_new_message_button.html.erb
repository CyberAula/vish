<% if not user_signed_in? or (current_subject != subject) %>
  <%= link_to(image_tag("icon-message.png",:class => "imgnoradio")+ t('  ')+ t('message.send'), new_message_path(:receiver => subject.slug), :class => "btn-gray", :remote=> false) %>
<% end %>

