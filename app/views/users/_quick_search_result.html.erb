<div class="quick_search">
  <div class="photo">
 <%= link_to(image_tag(user.logo.url(:medium)), user.subject) %>
  </div>
  <div class="data">
    <div class="name">
      <%= link_to(truncate_name(user.name), user.subject)%>
    </div>
    <div class="author"> 
      <%= contact_count(user) %>
    </div>
  </div>
</div>
