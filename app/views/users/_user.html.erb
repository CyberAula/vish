<li class="contact" id="<%= dom_id user %>">
  <%if user.is_mve %>
    <div class="badge-teacher-active">  </div>
  <%end%>
  <div class="container-img">
      <div class="dummy"></div>
      <div class="element">
      <div class="avatar">
        <%= link_to(image_tag(user.logo.url(:medium)), user.subject) %>
      </div>
      </div>
  </div>
<%# link_to excursion_thumb_for(excursion), excursion, :class => "img-box" %>
<!--INFO excursuion-->
  <div class="caption">
<!--Title -->
    <h6>
      <%= link_to(truncate_name(user.name), user.subject)%>
    </h6>
<!--Autor -->
      <div class="add">
        <%= contact_button(user) %>
      </div> 
    <!--____________________ 
    <div class="info-bottom">
      <hr class="soften">
      <%#= contact_count(user) %>
    </div>-->
  </div>
</li>










