<ul class="items">
    <% if profile_subject_is_current?%>
        <%= render partial: 'workshops/add_new' %>
    <% end %>
    <%= render partial: 'workshops/profile_workshops_page', :locals => { :scope => scope, :limit => limit, :page => page, :sort_by=> sort_by} %>
</ul>
